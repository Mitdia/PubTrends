To start the application one needs to install all libraries from the requirements.txt file
```
pip install -r requirements.txt
```
Then to run the app use the command:
```
bokeh start serve --show Visualizer.py
```
